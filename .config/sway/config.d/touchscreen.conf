# Mappatura del toggle: 
# 1. Automatico (se il tuo 2-in-1 lo supporta)
bindswitch tablet:on  exec ~/.local/bin/tablet-mode.sh
bindswitch tablet:off exec ~/.local/bin/tablet-mode.sh

# 2. Manuale (opzionale)
bindsym $mod+shift+t exec ~/.local/bin/tablet-mode.sh